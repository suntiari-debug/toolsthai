# Design Reference Notes: Tools Thai

## ภาพ Tools Thai-1 และ Tools Thai-2

ภาพอ้างอิงแสดงหน้าเครื่องมือเอกสารแบบสองคอลัมน์บนเดสก์ท็อป โดยแผงฟอร์มด้านซ้ายอยู่ใน card สีขาวมุมโค้ง มีเส้นขอบสีเทาอ่อนและเงาบาง ส่วนด้านขวาเป็นพื้นที่ทำงานและตัวอย่างเอกสารขนาดใหญ่บนพื้นหลังเทาอ่อน ทั้งหน้ามีช่องว่างกว้างและใช้ลำดับสายตาที่ชัดเจน

แผง “ดีไซน์เอกสาร” ใช้ตัวเลือก template แบบสามคอลัมน์, swatch สีทรงกลม และช่องอัปโหลดแบบเส้นประสำหรับโลโก้/ลายเซ็น/ตรายาง ปุ่ม action ด้านบนของพื้นที่ preview ใช้ปุ่ม PDF สีน้ำเงินเป็น primary action ตามด้วยปุ่มพิมพ์แบบรองและปุ่มรีเซ็ตแบบ text button

ตัวอย่างเอกสารด้านขวาเป็นกระดาษสีขาวเต็มพื้นที่ card มี padding ระดับใหญ่ หัวเอกสารสองฝั่ง เส้นคั่นหนา ตารางรายการสินค้า header สีดำ และกล่องข้อมูลเอกสารพื้นเทาอ่อน

บนมือถือ card “ข้อมูลเอกสาร” กว้างเกือบเต็มหน้าจอ ใช้ padding ประมาณ 24px, radius ราว 16px, label ขนาดเล็กแต่ชัดเจน และ grid สองคอลัมน์สำหรับวันออกเอกสาร/กำหนดชำระ โดย input มีความสูงสม่ำเสมอและไอคอนปฏิทินวางด้านขวา

## ภาพ Tools Thai-3 และ Tools Thai-4

ฟอร์ม “ผู้ขาย / ผู้ออกเอกสาร” และ “ลูกค้า / ผู้รับเอกสาร” ใช้ card เดี่ยวแบบเต็มความกว้างบนมือถือ มีหัวข้อ bold ชิดซ้าย พื้นที่ระหว่างหัวข้อกับ field โปร่งพอดี และใช้ช่องกรอกแบบขอบเทาอ่อน radius มน เส้นขอบ/เงาแบบนุ่มเพื่อแยกจากพื้นหลังโดยไม่หนักเกินไป

ทั้งสองฟอร์มจัดชื่อบริษัทและที่อยู่เป็น field เต็มแถว แล้วจัดข้อมูลที่เกี่ยวข้องเป็น grid สองคอลัมน์ โดยคงความกว้างของช่องให้เท่ากันและเว้นช่องว่างแคบสม่ำเสมอ ข้อความภาษาไทยมีขนาดอ่านง่ายและ label อยู่เหนือ field อย่างชัดเจน โดย card ของผู้ขายมีช่องโทรศัพท์และอีเมลเพิ่มในแถวสุดท้าย

## ภาพ Tools Thai-5 และ Tools Thai-6

ส่วน “รายการสินค้า / บริการ” เป็น card แบบเต็มความกว้าง หัวข้ออยู่ซ้ายและปุ่มเพิ่มรายการแบบ outline มีไอคอนบวกอยู่ขวา รายการย่อยแต่ละรายการมี card ซ้อนที่ขอบบาง มีป้ายลำดับรายการและไอคอนลบสีแดง textarea สำหรับชื่อสินค้าเต็มแถว และ grid สองคอลัมน์สำหรับจำนวน/ราคาต่อหน่วย

ส่วน “ภาษี” เป็น card แยก ใช้แถวตัวเลือกแบบข้อความอธิบายทางซ้ายและ switch ทางขวา เมื่อ VAT ถูกเปิด จะแสดง input อัตรา VAT ที่เล็กกว่าช่องกรอกหลักในบรรทัดถัดไป พร้อมเส้นแบ่งก่อนตัวเลือกหักภาษี ณ ที่จ่าย รูปแบบนี้ช่วยให้ส่วนที่เป็นทางเลือกมีลำดับและไม่หนาแน่นบนมือถือ

## ภาพ Tools Thai-7 และ Tools Thai-8

card “อื่นๆ” ใช้รูปแบบเดียวกับส่วนฟอร์มก่อนหน้า โดยมี textarea หมายเหตุเต็มแถวและ input ชื่อผู้มีอำนาจลงนามตามลำดับ มีระยะห่างระหว่าง label และ control ที่สม่ำเสมอ เพื่อให้เกิดจังหวะการกรอกข้อมูลที่อ่านง่าย

ภาพ preview เอกสารขนาดใหญ่ใช้ typography เอกสารจริงที่ชัดกว่าฟอร์ม: ชื่อบริษัทและชื่อประเภทเอกสารเด่นที่สุด หัวข้อรองรับการอ่านแบบสองฝั่ง มีเส้นคั่นสีเขียวอมฟ้าใต้ header กล่อง metadata ขนาดกระชับอยู่ด้านขวา และตารางรายการใช้แถว header สีดำ ตัวอักษรขาว เส้นตารางบาง และตัวเลขจัดชิดขวา รูปแบบเอกสารต้องคงความคมชัดเมื่อขยายหรือใช้สำหรับ PDF A4

## ภาพ Tools Thai-9

ส่วนท้ายของเอกสารรักษาพื้นที่ว่างมากเพื่อความเป็นทางการ ช่องจำนวนเงินตัวอักษรใช้กรอบเส้นประสีเขียวอมฟ้า กล่องสรุปยอดอยู่ด้านขวาเป็นพื้นเทาอ่อน เส้นคั่นยอดรวมเป็นสีเขียวอมฟ้า และยอดสุทธิใช้ตัวเลขหนาสีเดียวกัน ส่วนลายเซ็นจัดสองคอลัมน์ที่ฐานเอกสาร พร้อมเส้นเซ็นและข้อความกำกับกึ่งกลาง

## สถานะหน้า Quotation ปัจจุบัน (เดสก์ท็อป)

หน้า Quotation ปัจจุบันมีพื้นฐานแบบสองคอลัมน์และ preview A4 แล้ว แต่ card ฟอร์มรวมทุก section ไว้ในแผงเดียว ต่างจากภาพอ้างอิงที่สื่อสารการแบ่งกลุ่มข้อมูลเป็น card แยกชัดเจนกว่า พื้นที่ header ของหน้าและ action ยังมีลักษณะเป็นหน้า landing/product ปัจจุบัน มากกว่า workspace ที่เน้นปุ่มดาวน์โหลด PDF, พิมพ์ และรีเซ็ตเหนือ preview

preview มีลำดับข้อมูลต่างจากภาพอ้างอิง: metadata เอกสารปัจจุบันอยู่ใต้ header ในสามคอลัมน์ ขณะที่ภาพอ้างอิงวางข้อมูลลูกค้าด้านซ้ายและ metadata ในกล่องด้านขวา อีกทั้ง table header ควรเป็นสีดำและ total box ควรเป็นพื้นเทาอ่อน/ข้อความ teal ตามภาพแทนสี teal เต็มกล่อง

## สถานะหน้า Quotation ปัจจุบัน (มือถือ)

หน้า Quotation ปัจจุบันบนมือถือเรียง preview ก่อนฟอร์มและย่อกระดาษได้โดยไม่มี scrollbar ซึ่งเป็นพฤติกรรมที่เหมาะสมและจะคงไว้ อย่างไรก็ตาม card ฟอร์มรวม section หลายกลุ่ม ทำให้เส้นคั่นและ padding แน่นกว่าภาพอ้างอิง และ grid รายการสินค้าถูกยุบเป็นคอลัมน์เดี่ยวทั้งหมด แม้ภาพอ้างอิงต้องการให้คู่ข้อมูลสำคัญ เช่น จำนวน/ราคาต่อหน่วย และข้อมูลวันที่ ยังคงเป็นสองคอลัมน์ได้ในความกว้างประมาณ 415–430px

การปรับเป้าหมายคือเปลี่ยนส่วนฟอร์มเป็น card แยกต่อ section, เพิ่ม padding/radius ให้ใกล้ภาพ, รักษา grid สองคอลัมน์สำหรับคู่ข้อมูลที่สั้น และคง preview แบบย่อให้แสดงครบเหนือฟอร์มในมือถือ

## ขอบเขตฟีเจอร์เครื่องคำนวณที่ต้องคงไว้

หน้าเครื่องคำนวณใช้ component และ class เฉพาะกลุ่ม `calculator-*` แม้จะใช้ `shell`, `back-link` และ `field-grid` ร่วมกับหน้าเอกสาร การปรับครั้งนี้จะจำกัดกฎใหม่ไว้ใน `document-*`, `form-*`, `pdf-*` และ `item-editor*` เพื่อไม่เปลี่ยน logic การคำนวณ การจัด layout ผลลัพธ์ หรือการแสดงคำเตือน VAT ของเครื่องคำนวณเดิม

## ผลตรวจหลังปรับตามภาพอ้างอิง

บนเดสก์ท็อป หน้า Quotation เปลี่ยนเป็น workspace สองคอลัมน์ชัดเจน: แผงซ้ายเป็น card แยกตามข้อมูลและแผงขวาเป็น preview A4 ขนาดใหญ่ พร้อม toolbar ดาวน์โหลด PDF/พิมพ์/รีเซ็ตด้านบน ตัวอย่างเอกสารใช้ header สองฝั่ง, กล่อง metadata ด้านขวา, header ตารางสีดำ, กล่องยอดสรุปพื้นอ่อน และตำแหน่งลายเซ็นตามภาพอ้างอิงแล้ว

บนมือถือ preview ย่อแสดงครบโดยไม่มี scrollbar และฟอร์มแยกเป็น card เต็มความกว้างตามลำดับข้อมูล คู่ input ที่สั้น เช่น วันที่ จำนวน และราคา ยังคงสองคอลัมน์ได้ในหน้ากว้าง 430px ทั้งหมดใช้เฉพาะ class หน้าเอกสาร จึงไม่กระทบหน้าคำนวณเดิม

## ตรวจ regression รอบสุดท้าย

ช่อง “รายละเอียดเพิ่มเติม” ของรายการสินค้าแสดงสำหรับทุกรายการ แม้เริ่มต้นเป็นค่าว่าง จึงยังรองรับการกรอกรายละเอียดและการแสดงผลใน preview/PDF เช่นเดิม ผลตรวจรอบสุดท้ายบนเดสก์ท็อปและมือถือยืนยันว่า card, toolbar, preview A4 และ responsive grid ยังอยู่ในรูปแบบที่สอดคล้องกับภาพอ้างอิง

## ตรวจ Invoice ที่ใช้ layout ร่วมกัน

หน้า Invoice แสดง workspace, card form, toolbar, preview A4 และ responsive behavior ชุดเดียวกับหน้า Quotation ได้ถูกต้องทั้งเดสก์ท็อปและมือถือ โดยเปลี่ยนเฉพาะชื่อประเภทเอกสาร ข้อความ SEO และทางเลือกการทำเอกสารต่อเนื่องตามชนิดเอกสาร จึงยืนยันได้ว่าการปรับดีไซน์ไม่จำกัดอยู่เฉพาะหน้า Quotation

## เกณฑ์ typography เพิ่มเติมจากภาพอ้างอิง

Tools Thai-1 ใช้ฟอนต์ UI ไทยทรง humanist ที่อ่านง่าย น้ำหนัก regular ใน field/คำอธิบาย และ semibold สำหรับหัว card โดยไม่อัดแน่นเกินไป ขณะที่ Tools Thai-8 แสดงตัวอย่าง A4 ด้วยตัวอักษรที่ใหญ่และเข้มกว่าหน้าเว็บปัจจุบัน: ชื่อบริษัทและชื่อเอกสารเด่นชัด, รายละเอียดบริษัท/ลูกค้าอ่านได้ในระดับรอง, และข้อความตารางมีขนาดใกล้เคียงกับข้อมูล metadata ไม่เล็กจนเสียลำดับสายตา

เป้าหมายการปรับคือยก scale typography ภายในกระดาษ A4 ขึ้นเป็นระดับอ้างอิง โดยเฉพาะ company name, document title, customer/meta content, header table, cell body และ total ขณะยังคงพื้นที่ว่างและ export A4 จริงไว้

## ผลเทียบกับ preview ปัจจุบัน

preview ปัจจุบันใช้ฟอนต์ Noto Sans Thai ได้ใกล้เคียงลักษณะอักษรไทยของภาพอ้างอิง แต่ขนาดข้อความในกระดาษ A4 เล็กกว่าชัดเจนเมื่อดูในพื้นที่หน้าเว็บ โดยเฉพาะ metadata, รายการตาราง, จำนวนเงินตัวอักษร และลายเซ็น ภาพอ้างอิงใช้ข้อความเนื้อหาระดับประมาณ 14–16px ในสัดส่วน A4, header table และ label ระดับ 13–14px, ชื่อบริษัท/ชื่อเอกสารระดับประมาณ 28–34px และยอดสุทธิเด่นระดับประมาณ 22–25px

การปรับจะเปลี่ยน font stack ของ workspace/form เป็น Noto Sans Thai เป็นหลัก เพิ่มน้ำหนักหัวข้ออย่างพอดี และขยาย typography ของ preview โดยไม่เปลี่ยน scale CSS ของกระดาษบนจอ เพื่อให้ความกว้างและพื้นที่ preview ยังคงพอดีกับ layout เดิม

## ผลตรวจหลังปรับ typography

หน้าเอกสารใช้ Sarabun เป็นฟอนต์หลักของ workspace/form เพื่อให้รูปทรงข้อความใกล้ภาพอ้างอิงมากขึ้น ขนาดตัวอักษรของ preview A4 ถูกยกขึ้นในทุกลำดับสำคัญ ได้แก่ ชื่อบริษัท ชื่อเอกสาร ข้อมูลลูกค้า/metadata ตาราง ยอดรวม และช่องลายเซ็น โดยยังรักษากระดาษ A4 เต็มพื้นที่ preview บนเดสก์ท็อป และย่อเต็มความกว้างโดยไม่มี scrollbar บนมือถือ

## ผลตรวจการขยาย preview บนหน้าจอ

หลังแก้ลำดับ CSS ให้ scale ใหม่มีผลจริง ตัวอย่าง A4 บนเดสก์ท็อปขยายจากระดับเดิมจนเติมพื้นที่ด้านขวาเกือบเต็มความกว้าง ทำให้ข้อความภายในอ่านได้ชัดขึ้นและใกล้สัดส่วน Tools Thai-8/9 ส่วนบนมือถือยังคงย่อพอดีความกว้างของ container และไม่เกิด scrollbar ภายใน
